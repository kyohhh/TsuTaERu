<div class='topPage'>
  <div class="container">
    <h1>通知代理サービス</h1>
    <p>身近にいる人へ感謝の気持ちを「伝えたい」けど照れくさい・時間がない。そんな方の代理をいたします。
    </p>
    
    <%= form_with model: @inquiry, url: { action: :confirm }, html: { class: 'form' }, local: true do |f| %>
    <%= render 'layouts/error_messages', model: f.object %>

      <div class="form-group">
       <%= f.label :name %>
        <%= f.text_field :name, autofocus: true, class: "form-control", placeholder: "お名前" %>
      </div>
      <div class="form-group">
       <%= f.label :email %>
        <%= f.text_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス" %>
      </div>
      <div class="form-group">
        <%= f.label :relationship %>
        <%= f.select(:relationship, [["親", "親"], ["夫婦", "夫婦"], ["恋人", "恋人"], ["友人", "友人"], ["上司", "上司"], ["先輩", "先輩"]], {}, class: "form-control") %>
      </div>
      <div class="form-group">
        <%= f.label :content %>
        <%= f.text_area :content, class: "form-control", placeholder: "伝えたいことを入力してください" %>
      </div>
       <div class="text-center"><%= f.submit '確認画面へ', class: 'btn btn-primary' %></div>
    <% end %>
  </div>
</div>
